name: CI Accord Rails

on:
  push:
    branches: [main]
    paths:
      - "packages/accord-rails/**"
      - "packages/accord-core/**"
      - ".github/workflows/ci-accord-rails.yml"
      - "package.json"
      - "package-lock.json"
  pull_request:
    branches: [main]
    paths:
      - "packages/accord-rails/**"
      - "packages/accord-core/**"
      - ".github/workflows/ci-accord-rails.yml"
      - "package.json"
      - "package-lock.json"

jobs:
  build:
    name: Build, typecheck & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install workspace
        run: npm install

      - name: Build accord-core (accord-rails depends on its dist)
        run: npm run build -w @accord-protocol/core

      - name: Typecheck
        run: npm run typecheck -w @accord-protocol/rails

      - name: Test
        run: npm test -w @accord-protocol/rails

      - name: Build
        run: npm run build -w @accord-protocol/rails
