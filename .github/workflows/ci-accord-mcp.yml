name: CI Accord MCP

on:
  push:
    branches: [main]
    paths:
      - "packages/accord-mcp/**"
      - "packages/accord-core/**"
      - ".github/workflows/ci-accord-mcp.yml"
      - "package.json"
      - "package-lock.json"
  pull_request:
    branches: [main]
    paths:
      - "packages/accord-mcp/**"
      - "packages/accord-core/**"
      - ".github/workflows/ci-accord-mcp.yml"
      - "package.json"
      - "package-lock.json"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    name: Build, typecheck & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install workspace
        run: npm install

      - name: Build accord-core (accord-mcp depends on its dist)
        run: npm run build -w @accord-protocol/core

      - name: Typecheck
        run: npm run typecheck -w @accord-protocol/mcp

      - name: Test
        run: npm test -w @accord-protocol/mcp

      - name: Build
        run: npm run build -w @accord-protocol/mcp
