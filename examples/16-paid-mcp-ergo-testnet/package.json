{
  "name": "accord-paid-mcp-ergo-testnet-demo",
  "version": "0.5.0",
  "private": true,
  "description": "End-to-end paid-MCP demo on Ergo testnet — buyer creates a real Note via ergo-agent-pay, seller's @accord-protocol/rails-ergo verifies and redeems it on-chain. The non-mock counterpart of example 15.",
  "type": "module",
  "scripts": {
    "setup:reserve": "tsx scripts/setup-reserve.ts",
    "dev": "tsx run-demo.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@accord-protocol/core": "^0.4.1",
    "@accord-protocol/mcp": "^0.4.1",
    "@accord-protocol/rails": "^0.4.1",
    "@accord-protocol/rails-ergo": "^0.4.1",
    "ergo-agent-pay": "^0.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
